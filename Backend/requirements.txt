# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── Database ──────────────────────────────────────────────────────────────────
SQLAlchemy
PyMySQL
python-dotenv

# ── Google AI  (NEW SDK only — do NOT add google-generativeai or
#    google-ai-generativelanguage; they conflict with google-genai) ────────────
google-genai

# ── Security ──────────────────────────────────────────────────────────────────
# passlib's bcrypt self-test breaks on bcrypt>=4.1 (ValueError: password
# cannot be longer than 72 bytes) — pinning avoids the crash seen on /register.
passlib[bcrypt]
bcrypt==4.0.1
pydantic
python-multipart

# ── Scientific (required by /simulate to generate GIF animations) ─────────────
numpy
matplotlib
pillow

# ── Misc ──────────────────────────────────────────────────────────────────────
requests